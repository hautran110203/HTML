let inputWidth = prompt("Enter the width");
let inputHeight = prompt("Enter the height");

let width1 = parseInt(inputWidth)
let height1 = parseInt(inputHeight)

let area1 = width1 * height1
document.write("Area = "+ area1)

